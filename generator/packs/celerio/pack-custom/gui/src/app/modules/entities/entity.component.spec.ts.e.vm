$output.file("gui/src/app/modules/${entity.model.var}/${entity.model.var}.component.spec.ts")##
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ${entity.name}Component } from './${entity.model.var}.component';

describe('${entity.name}Component', () => {
  let component: ${entity.name}Component;
  let fixture: ComponentFixture<${entity.name}Component>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ${entity.name}Component ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(${entity.name}Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
